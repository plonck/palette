{
  "schemaVersion": 1,
  "id": "palette",
  "version": "${version}",
  "name": "Palette",
  "description": "Generates tables for matching blocks to map colors",
  "license": "APACHE-2.0",
  "environment": "server",
  "accessWidener": "palette.accesswidener",
  "entrypoints": {
    "main": ["org.plonck.palette.PaletteMod"]
  },
  "depends": {
    "minecraft": "${version_minecraft}",
    "fabricloader": ">=${version_fabricloader}"
  }
}
